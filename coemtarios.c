#include <stdio.h>
// A linha acima inclui a biblioteca padrão de entrada e saída (standard input/output).
// Ela é necessária para usar a função printf(), que imprime texto na tela.

int main(void)
// O programa começa a ser executado pela função main.
// Todo programa em C precisa ter uma função main.
{
    printf("Hello, World!\n");
    // printf imprime a mensagem "Hello, World!" na tela.
    // O \n é um caractere especial que faz a quebra de linha
    // (ou seja, move o cursor para a próxima linha).

    return 0;
    // O return 0 indica que o programa terminou corretamente.
    // O valor 0 é retornado para o sistema operacional.